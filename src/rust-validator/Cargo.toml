[package]
name = "metro_validator"
version = "0.1.0"
edition = "2021"

[lib]
name = "metro_validator"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.22.6", features = ["auto-initialize"] }
